// Utilities library
#include <cstdlib>            // general purpose utilities: program control, dynamic memory allocation, 
                             //   random numbers, sort and search
#include <csignal>           // Functions and macro constants for signal management
#include <csetjmp>           // Macro (and function) that saves (and jumps) to an execution context
#include <cstdarg>           // Handling of variable length argument lists
#include <typeinfo>          // Runtime type information utilities
#include <typeindex>         // std::type_index
#include <type_traits>       // Compile-type type information
#include <bitset>            // std::bitset class template
#include <functional>        // Function objects, Function invocations, Bind operations 
                             //   and Reference wrappers
#include <utility>           // Various utility components
#include <ctime>             // C-style time/date utilities
#include <chrono>            // C++ time utilities
#include <cstddef>           // standard macros and typedefs
#include <initializer_list>  // std::initializer_list class template
#include <tuple>             // std::tuple class template
#include <any>               // std::any class               ----> Cont.
#include <optional>          // std::optional class template ----> Cont.
#include <variant>           // std::variant class template  ----> Cont.

// Dynamic memory allocation
#include <new>               // Low-level memory management utilities
#include <memory>            // Higher level memory management utilites
#include <scoped_allocator>  // Nested allocator class
#include <memory_resource>   // Polymorphic allocators and memory resources

// Numeric limits
#include <climits>           // limits of integral types
#include <cfloat>            // limits of float typesgit
#include <cstdint>           // fixed-size types and limits of other types
#include <cinttypes>         // formatting macros, intmax_t and unitmax_t math and conversions
#include <limits>            // standardized way to query properties of arithmentic types

// Error handling
#include <exception>         // Exception handling utilities
#include <stdexcept>         // Standard exception objects
#include <cassert>           // Conditionally compiled macro that compares its argument to zero
#include <system_error>      // defines std::error_code, a platform-dependent error code
#include <cerrno>            // Macro containing the last error number 

// Strings library
#include <cctype>            // Functions to determine the type contained in character data
#include <cwctype>           // Functions to determine the type contained in wide character data
#include <cstring>           // various narrow character string handling functions
#include <cwchar>            // various wide and multibyte string handling functions
#include <cuchar>            // C-style Unicode character conversion functions
#include <string>            // std::basic_string class template
#include <string_view>       // std::basic_string_view class template  ---> Cont.

// Containers library
#include <array>             // std::array container
#include <vector>            // std::vector container
#include <deque>             // std::deque container
#include <list>              // std::list containers
#include <forward_list>      // std::forward_list containers
#include <set>               // std::set and std::multiset associative containers
#include <map>               // std::map and std::multimap associative containers
#include <unordered_set>     // std::unordered_set and std::unordered_multiset 
                             //   unordered associative containers 
#include <unordered_map>     // std::unordered_map and std::unordered_multimap
                             //   unordered associative containers
#include <stack>             // std::stack container adaptor
#include <queue>             // std::queue and std::priority_queue container adaptors
 
// Algorithms library
#include <algorithm>         // Algorithms that operate on containers
#include <execution>         // Predefined execution policies for parallel versions of the algorithms

// Iterators library
#include <iterator>          // Container iterators

// Numerics library
#include <cmath>             // Common mathematics functions
#include <complex>           // Complex number type
#include <valarray>          // Class for representing and manipulating arrays of values
#include <random>            // Random number generators and distributions
#include <numeric>           // Numeric operations on values in containers
#include <ratio>             // Compile-time rational arithmentic
#include <cfenv>             // Floating-point environment access functions

// Input/output library
#include <iosfwd>            // forward declarations of all classes in the input/output library
#include <ios>               // std::ios_base class, std::basic_ios class template and several typedefs
#include <istream>           // std::basic_istream class template and several typedefs
#include <ostream>           // std::basic_ostream, std::basic_iostream class templates and several typedefs
#include <iostream>          // several standard stream objects
#include <fstream>           // std::basic_fstream, std::basic_ifstream, std::basic_ofstream class templates
                             //   and several typedefs
#include <sstream>           // std::basic_stringstream, std::basic_istringstream, std::basic_ostringstream
                             //   class templates and several typedefs
#include <strstream>         // std::strstream, std::istrestream, std::ostrstream
#include <iomanip>           // Helper functions to control the format of input and output
#include <streambuf>         // std::basic_streambuf class template
#include <cstdio>            // C-style input-output functions

// Localization library
#include <locale>            // Localization utilities
#include <clocale>           // C localization utilities
#include <codecvt>           // Unicode conversion facilities

// Regular Expressions library
#include <regex>             // Classes, algorithms and iterators to support regular expression processing

// Atomic Operations library
#include <atomic>            // Atomic operations library

// Thread support library
#include <thread>            // std::thread class and supporting functions
#include <mutex>             // mutial exclusion primitives
#include <shared_mutex>      // shared mutual exclusion primitives
#include <future>            // primitives for asynchronous computations
#include <condition_variable>// thread waiting conditions

// Filesystem library
#include <filesystem>        // std::path class and supporing functions

int main()
{

}